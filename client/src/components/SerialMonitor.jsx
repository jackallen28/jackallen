// TODO: connects to useSerial hook (connect/disconnect/send). Displays incoming
//       serial data in a scrolling log and provides a text input to send commands.
export default function SerialMonitor() {
  return (
    <div className="h-full bg-[#0d0d1a] border-t border-[#0f3460] p-2">
      <p className="text-xs text-gray-500">SerialMonitor</p>
    </div>
  )
}
