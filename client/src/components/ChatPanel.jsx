// TODO: connects to claude.js sendMessage(). Displays conversation history,
//       streams responses from POST /api/chat, and can insert code snippets into CodeEditor.
export default function ChatPanel() {
  return (
    <div className="h-full bg-[#16213e] border-l border-[#0f3460] p-2">
      <p className="text-xs text-gray-500">ChatPanel</p>
    </div>
  )
}
